grammar @Model.GrammarName;

options {
    language = 'CSharp';
}

program: statement+ EOF;

statement: @Raw($"{string.Join(" | ", Model.Rules.Select(r => r.Name).ToArray())};")

@foreach (var rule in Model.Rules)
{
    @Raw($"{rule.Name}: '{rule.InputExpression}';\n")
}
